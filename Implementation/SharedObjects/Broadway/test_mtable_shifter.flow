stages:
  Stage 1:
    actors:
      MTableShift1:
        parent: MTableShift
        in:
          seed:
            const: '7'
          mtable:
            const: shifter_mtable
          mtableKey:
            const: value
            schema: string
          value:
            const: '1'
            schema: string
      MTableShift2:
        parent: MTableShift
        in:
          seed:
            const: '1'
          mtable:
            const: shifter_mtable_str
          mtableKey:
            const: value
            schema: string
          value:
            const: AR
            schema: string
  Stage 2: {
    }
  Stage 3: {
    }
  Stage 4: {
    }
